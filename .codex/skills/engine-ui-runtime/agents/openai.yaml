# Configures the Codex agent prompt for Engine runtime UI work.

interface:
  display_name: "Engine UI Runtime"
  short_description: "Runtime UI workflow for Engine"
  default_prompt: "Use $engine-ui-runtime to implement the next runtime UI task."
