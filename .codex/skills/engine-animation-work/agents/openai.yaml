# Configures the Codex agent prompt for Engine animation work.

interface:
  display_name: "Engine Animation Work"
  short_description: "Animation-system workflow for Engine"
  default_prompt: "Use $engine-animation-work to implement the next animation system task."
